import { StyleSheet } from 'react-native';

export const ViewStyles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff'
    },
    listContainer: {
        marginTop: 13,
        borderTopWidth: 0
    }
});
